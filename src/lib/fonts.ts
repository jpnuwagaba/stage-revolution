import { Rock_Salt, Space_Grotesk } from "next/font/google";

export const spacegrotesk = Space_Grotesk({ subsets: ["latin"] });
export const rocksalt = Rock_Salt({
  subsets: ["latin"],
  weight: "400"
})